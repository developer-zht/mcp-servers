{
  // 继承根配置
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // 输出目录
    "outDir": "./dist",
    "rootDir": "./src",

    // 启用复合项目模式，支持 Project References
    // 必须启用才能使用项目引用和增量编译
    // 但是，应用模式: 不需要启用复合项目模式
    "composite": false,
    // composite: true ⇒ 必须能生成 .d.ts
    // 但是，应用模式: 不需要生成 .d.ts (因为是可执行程序,不是库)
    "declaration": false,
    "declarationMap": false,
    // @types 包提供的全局类型声明
    "types": ["node", "vitest/globals"],
    // 会覆盖掉 "extends": "../../tsconfig.json" 中的 "baseUrl" 和 "paths"
    "baseUrl": ".",
    "paths": {
      "@mcp-servers/shared": ["../shared/src/index.ts"],
      "@mcp-servers/shared/*": ["../shared/src/*"],
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],
  // 引用依赖的包 (TypeScript Project References)
  "references": [{ "path": "../shared" }]
}
