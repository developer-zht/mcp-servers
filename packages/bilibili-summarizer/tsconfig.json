{
  // 继承根配置
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    // 输出目录
    "outDir": "./dist",
    "rootDir": "./src",
    // 应用模式: 不需要生成 .d.ts (因为是可执行程序,不是库)
    "declaration": false,
    "declarationMap": false,

    // 启用复合项目模式，支持 Project References
    // 必须启用才能使用项目引用和增量编译
    "composite": true
  },
  "include": ["src/**/*"],
  // 引用依赖的包 (TypeScript Project References)
  "references": [{ "path": "../shared" }]
}
